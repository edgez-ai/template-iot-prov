# Mobile companion

The Expo app supports the same Appwrite Auth, built-in Devices API, MQTT
credential creation, and direct TablesDB telemetry reads as the Next.js site.
Its Expo deep-link scheme is `edgez-devtools`, so application links begin with
`edgez-devtools://`.
